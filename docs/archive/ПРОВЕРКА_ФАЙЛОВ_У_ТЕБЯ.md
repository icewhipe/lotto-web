# 🔍 ПРОВЕРКА - ЧТО У ТЕБЯ В ФАЙЛАХ?

## Выполни эти команды и отправь результат:

```bash
cd /Users/vasiliidyahenko/Documents/GitHub/lotto-web

# 1. Сколько строк в каждом файле?
echo "=== GradesView ==="
wc -l src/components/dashboard/GradesView.tsx

echo "=== ScheduleView ==="
wc -l src/components/dashboard/ScheduleView.tsx

echo "=== AttendanceView ==="
wc -l src/components/dashboard/AttendanceView.tsx

echo "=== StudentChat ==="
wc -l src/components/student/StudentChat.tsx

# 2. Что в GradesView?
echo "=== Содержимое GradesView ==="
cat src/components/dashboard/GradesView.tsx

# 3. Что в ScheduleView?
echo "=== Содержимое ScheduleView ==="
cat src/components/dashboard/ScheduleView.tsx
```

**ОТПРАВЬ МНЕ ВЕСЬ ВЫВОД!**
